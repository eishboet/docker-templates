<?xml version="1.0" encoding="utf-8"?>
<Container version="0.1">
  <Name>nginx</Name>
  <Overview>
    Nginx is a web server which can also be used as a reverse proxy, load balancer, mail
    proxy and HTTP cache.

    Nginx is free and open-source software, released under the terms of the 2-clause BSD
    license. A large fraction of web servers use NGINX,often as a load balancer.
  </Overview>
  <Registry>
    http://localhost:5000/registry/nginx
  </Registry>
  <Repository>
    Docker Registry username and containers name user/containers
  </Repository>
  <Privileged>
    False
  </Privileged>
  <Support>
    URL to the containers support page
  </Support>
  <Network>
      bridge
  </Network>
  <WebUI> # Holds a mask for the web management interface of the containers main application
    http://localhost:[PORT:80]/index.html
  </WebUI>
  <Icon>
    https://github.com/eishboet/docker-templates/tree/master/icons/nginx-icon.png
  </Icon>
  <Category>
    Network:Web, Status:Beta
  </Category>
  <Config
    Type="Path"
    Name="nginx.conf"
    Target="/etc/nginx/"
    Default="/etc/nginx/"
    Mode="ro"
    Description="Path to nginx.conf"
    Required="true">
      /mnt/usr/appdata/nginx/nginx.conf
  </Config>
  <Config
    Type="Path"
    Name="fastcgi_params"
    Target="/etc/nginx/"
    Default="/etc/nginx/"
    Mode="ro"
    Description="fastcgi_params"
    Required="true">
      /mnt/usr/appdata/nginx/fastcgi_params
  </Config>
  <Config
    Type="Path"
    Name="/var/www/html/"
    Target="/var/www/html/"
    Default="/var/www/html/"
    Mode="ro"
    Description="www folder"
    Required="true">
      /mnt/usr/appdata/nginx/www
  </Config>
  <Config
    Type="Port"
    Name="nginx http"
    Target="80"
    Default="80"
    Mode="tcp"
    Description="http port 80"
    Display="always"
    Required="true">
      80
  </Config>
 </Container>
